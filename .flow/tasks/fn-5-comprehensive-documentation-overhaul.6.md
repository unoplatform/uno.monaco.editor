# fn-5-comprehensive-documentation-overhaul.6 Add XML documentation to generated Monaco types

## Description
Decide and document the XML documentation strategy for the ~40+ generated Monaco type files in `MonacoEditorComponent/Monaco/`. This task runs after fn-4 (type generation pipeline) completes and produces a frozen baseline from the emitter output.

**Size:** S
**Files:** `docs/generated-type-docs-strategy.md`

## Approach

1. **Baseline the fn-4.5 emitter output**: Inventory all generated `.cs` files, count public symbols, assess existing doc coverage
2. **Evaluate strategies**:
   - **Strategy A — Emitter generates XML docs**: Modify the type emitter to emit XML docs derived from TypeScript JSDoc comments in `monaco.d.ts`. Keeps docs in sync automatically.
   - **Strategy B — Post-processing step**: A separate tool/script merges hand-written XML docs with emitter output after regeneration. Allows richer docs than JSDoc provides.
   - **Strategy C — Hand-written with preservation convention**: Add XML docs manually with a convention (e.g., separate `.xmldoc` files) that survives regeneration.
3. **Decision criteria**: Maintenance burden, doc quality, JSDoc coverage in `monaco.d.ts`, regeneration frequency
4. **Document the chosen strategy** with rationale, implementation steps, and acceptance criteria for task 8

## Key context

- fn-4.5 (type emitter) must be complete before this task starts
- `StandaloneEditorConstructionOptions` already has 119 summary doc entries — assess what the emitter preserves
- Many enum files have only type-level docs, no per-member docs
- Core types needing thorough docs: `Position`, `Range`, `Selection`, `IPosition`, `IRange`, `Uri`, `IMarkdownString`
- ~35+ enums, ~40+ classes/interfaces in `Monaco/Editor/` and `Monaco/Languages/`
## Approach

Two potential strategies (decide after fn-4.5 completes):

**Strategy A — Emitter generates XML docs**: Modify the type emitter (fn-4.5 output) to emit XML docs derived from TypeScript JSDoc comments in the Monaco `.d.ts` file. This keeps docs in sync automatically.

**Strategy B — Hand-written XML docs**: Add XML docs manually to the emitter output. Requires a post-processing step or convention to preserve docs across regeneration.

**Regardless of strategy:**
- Core types needing thorough docs: `Position`, `Range`, `Selection`, `IPosition`, `IRange`, `Uri`, `IMarkdownString`
- Editor types: `StandaloneEditorConstructionOptions` (84 public members — largest single file), `IEditorOptions` (422 comment lines), `IModelDeltaDecoration`, `IModelDecorationOptions`, `Marker`, `MarkerData`, `FindMatch`, `ContextKey`
- Language types: `CompletionItem`, `CompletionList`, `CodeAction`, `Hover`, `TextEdit`, `WorkspaceEdit`, `Command`
- Provider interfaces: `HoverProvider`, `CompletionItemProvider`, `CodeActionProvider`, `CodeLensProvider`, `DocumentColorProvider`
- Enums (~35+): `MarkerSeverity`, `MarkerTag`, `SelectionDirection`, `CompletionItemKind`, `CursorBlinking`, `WordWrap`, `AutoIndent`, etc.
- Use `<see href="..."/>` to cross-reference upstream Monaco TypeDoc API for each type
- Mark generated files with `// <auto-generated />` header if not already present

## Key context

- fn-4.5 (type emitter) is in_progress — output structure not yet finalized
- `StandaloneEditorConstructionOptions` already has 119 summary doc entries — assess what's missing
- Many enum files have only type-level docs, no per-member docs
- Per user direction: "details around those tasks should be flushed out right after [fn-4]"
- Consider whether `<inheritdoc/>` can flow from interface types to implementations

## Open questions (resolve after fn-4)

- Does the emitter preserve existing XML docs during regeneration?
- Can TypeScript JSDoc from `monaco.d.ts` be extracted and mapped to C# XML docs?
- Should generated files enable `GenerateDocumentationFile` separately?
## Acceptance
- [ ] Strategy document exists at `docs/generated-type-docs-strategy.md`
- [ ] fn-4.5 emitter output baselined (file count, public symbol count, existing doc coverage)
- [ ] At least 2 strategies evaluated with pros/cons
- [ ] Chosen strategy documented with rationale
- [ ] Implementation steps for task 8 clearly defined
- [ ] Acceptance criteria for task 8 derived from the chosen strategy
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
