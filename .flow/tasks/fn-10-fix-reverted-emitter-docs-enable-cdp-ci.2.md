# fn-10-fix-reverted-emitter-docs-enable-cdp-ci.2 Regenerate Monaco types with XML docs and re-enable CS1591

## Description
Re-run the full type generation pipeline after emitter edge cases are fixed, producing Monaco `.cs` files with XML documentation. Then re-enable CS1591 warnings in `MonacoEditorComponent/Monaco/.editorconfig`. Update CHANGELOG.md with Unreleased entries for all fn-10 changes.

**Size:** M
**Files:** `MonacoEditorComponent/Monaco/**/*.cs` (generated), `MonacoEditorComponent/Monaco/.editorconfig`, `docs/generated-type-docs-strategy.md`, `CHANGELOG.md`

## Approach
- Run the type extractor: `npx tsx tools/monaco-type-extractor/src/index.ts -- node_modules/monaco-editor/monaco.d.ts -o tools/monaco-type-extractor/output/model.json`
- Run the emitter: `dotnet run --project tools/MonacoTypeEmitter -- --input tools/monaco-type-extractor/output/model.json --output MonacoEditorComponent/Monaco/`
- Verify generated files have `// <auto-generated />` headers, `<summary>` docs, `<remarks>` with TypeDoc URLs, and enum member docs
- Change `Monaco/.editorconfig` CS1591 severity from `none` to `warning` (or remove the line) so builds enforce XML docs
- Build with `dotnet build MonacoEditorComponent.slnx` to verify compilation
- Update `docs/generated-type-docs-strategy.md`: mark acceptance criteria as complete; explicitly mark the known TypeDoc URL pattern drift (`editor.*` vs `editor_editor_api.*`) as **deferred** with a tracked follow-up TODO
- Update `CHANGELOG.md` Unreleased section with entries for: emitter edge-case fixes (from fn-10.1), XML doc regeneration, CS1591 re-enablement, Desktop CDP CI enablement (from fn-10.3), WSL2 profile (from fn-10.4)

## Key context
- The verified snapshot files in `tools/MonacoTypeEmitter.Tests/Snapshots/Verified/` show the expected output format
- Current `.editorconfig` at `MonacoEditorComponent/Monaco/.editorconfig:12` suppresses CS1591 as `none`
- `[LanguagesHelper*.cs]` override at line 15-16 re-enables CS1591 for hand-written files — preserve this
- This effectively completes fn-5.8 (reset to todo)
- TypeDoc URL pattern drift is a known issue documented in `docs/generated-type-docs-strategy.md` — do NOT block regeneration on resolving it; explicitly defer with tracked follow-up
## Acceptance
- [ ] All generated Monaco `.cs` files have `// <auto-generated />` header
- [ ] Generated types have `<summary>` XML docs derived from TypeScript JSDoc
- [ ] Core types have `<remarks>` with TypeDoc URLs
- [ ] Enum types have per-member documentation
- [ ] `Monaco/.editorconfig` CS1591 severity changed from `none` to `warning`
- [ ] `[LanguagesHelper*.cs]` override preserved
- [ ] `dotnet build MonacoEditorComponent.slnx` compiles without CS1591 errors
- [ ] `docs/generated-type-docs-strategy.md` acceptance criteria updated; TypeDoc URL pattern drift explicitly marked as deferred with TODO
- [ ] `CHANGELOG.md` Unreleased section updated with entries for: emitter edge-case fixes, XML doc regeneration, Desktop CDP CI enablement, WSL2 profile
## Done summary
TBD

## Evidence
- Commits:
- Tests:
- PRs:
